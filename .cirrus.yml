freebsd_instance:
  image: freebsd-12-0-release-amd64

task:
  name: FreeBSD test
  install_script: pkg install -y augeas pkgconf tcl86
  test_script: ./configure --with-tcl=/usr/local/lib/tcl8.6 && make test
